version: "3.8"
services:
    web:
        build: .
        ports:
            - "5000:80"
        depends_on:
            - db
    db:
        image: "microsoft/mssql-server-linux"
        environment:
            SA_PASSWORD: ""
            ACCEPT_EULA: "Y"
        ports: 
          - "1433:1433"
